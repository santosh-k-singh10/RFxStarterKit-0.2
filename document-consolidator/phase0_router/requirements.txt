# Phase 0 — Document Router Agent
# RFP Analyzer Multi-Agent System
# Install: pip install -r ../../common/requirements-base.txt -r requirements.txt

# Base dependencies are in ../../common/requirements-base.txt
# This file contains only Phase0-specific dependencies

# File upload support
python-multipart>=0.0.9   # required for FastAPI file uploads

# Document extraction
pypdf>=4.2.0              # PDF text extraction
python-docx>=1.1.2        # DOCX extraction
openpyxl>=3.1.2           # XLSX extraction
