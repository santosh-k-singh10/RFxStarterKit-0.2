# ============================================================================
# RFxStarterKit - Root Requirements
# ============================================================================
# Installs all dependencies for the full kit in one shot:
#   pip install -r requirements.txt
#
# For module-specific installs:
#   Base only:    pip install -r common/requirements-base.txt
#   RFP Analyzer: pip install -r common/requirements-base.txt -r rfp-analyzer/requirements.txt
#   Phase 0:      pip install -r common/requirements-base.txt -r document-consolidator/phase0_router/requirements.txt
# ============================================================================

-r common/requirements-base.txt
-r rfp-analyzer/requirements.txt
-r document-consolidator/phase0_router/requirements.txt
